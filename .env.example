NODE_ENV=production
PUBLIC_ORIGIN=https://your-domain.com
APP_PASSWORD=change_this_app_password
PORT=4173

# Optional operations config
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=20
MAX_EXPORT_BYTES=31457280
EXPORT_MAX_ITEMS=50
EXPORT_TTL_MS=600000
# LLM_CONFIG_PATH=/absolute/path/to/llm-config.json

# LLM Base URL / model / API Key are configured from the web UI after login.
